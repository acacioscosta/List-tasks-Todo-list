import { StyleSheet } from 'react-native'
import { styles as global_styles } from '../../global_styles'

const styles = StyleSheet.create({
  container: global_styles.container
})

export default styles